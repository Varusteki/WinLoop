### 【功能定位】
适用于windows平台的快捷窗口管理工具，按住鼠标中键出现操作菜单，移动鼠标指针选择后，松开中键即可完成预设的窗口操作，如切换最小化窗口、左半屏、回到桌面等。

### 【参考项目】
1. 功能实现：https://github.com/MrKai77/Loop
2. windows平台实现技术栈：https://github.com/yingDev/WGestures

### 【主要技术栈】
技术设计文档：./WinLoop_Technical_Design.md
- C#
- .NET Core 3.1
- WPF：用于创建图形界面
- Windows API：用于窗口管理和系统托盘

### 【版本控制逻辑】
项目文件按大版本归档。每次进行新的大版本改动前，上一个大版本代码备份到历史归档目录。
（说明：早期规划的 `./version` 目录未实际启用；当前构建历史由 `./build` 下的带时间戳目录承载，
Git 提交历史即代码版本。）

### 【测试用例与验证】
1. 运行时自检（自动化，推荐优先跑）：
    - `WinLoop.exe --fontcheck <日志文件路径>`：验证内嵌字体能否解析、字重是否落到不同字面
    - `WinLoop.exe --settingscheck <输出目录>`：构造设置窗口、逐页截图，
      断言布局与控件尺寸，报告写在 `<输出目录>/report.txt`
    - 详细说明见技术设计文档第 6.3 节
2. 人工测试验证：
    - 编译结果测试：运行 `./build/<版本>/WinLoop.exe`，检查是否正常运行
    - 安装包测试：运行 `./release/WinLoop_<版本>.exe`，检查是否正常安装并运行

（说明：早期规划的 `./testcase` 目录未实际启用，验证依赖上述运行时自检 + 人工测试。）

### 【编译与发布】
版本号规则：大版本+日期+时间，例如 V0.2-202609171737
1. 构建：运行 `job-build.ps1`（底层 `build.ps1`），产物在 `./build` 目录下，
   结束后自动打开资源管理器并选中 exe
2. 打包：运行 `job-pack.ps1`（底层 `release.ps1`），安装包在 `./release` 目录下，
   结束后自动打开资源管理器并选中安装包

> ⚠️ 打包前务必先清 `WinLoop/bin` 与 `WinLoop/obj`，否则增量构建会带入上一版残留 dll。

### 【功能描述】
#### 基础功能
1. 按住鼠标中键达到设置的时长出现操作菜单，移动鼠标指针选择后，松开中键即可完成预设的窗口操作。
2. 操作菜单默认展示基础环形菜单样式，可在设置中切换其他菜单样式。

#### 设置
通过系统托盘图标打开设置窗口（双击托盘图标，或右键 → 设置），
设置窗口按设置项类别分页签显示，共 4 页：

**（1）菜单样式**
菜单选项包括：圆环、八角星（CS爆头）、蜘蛛网、八卦图 —— 全部可选，切换后实时预览。
各样式设置项：

| 样式 | 设置项 | 默认值 |
|---|---|---|
| 圆环 | 外圈半径 | 50 |
| | 内圈半径 | 28 |
| | 圆环颜色 | `#C0C0C0` |
| | 高亮颜色 | `#007AFF` |
| 八角星 | 大小（滑块） | 100% |
| | 线条颜色 | `#1A1A1A` |
| | 底色 | `#FCFCFC` |
| | 高亮颜色 | `#D4AF37` |
| 蜘蛛网 | 外圈半径 | 70 |
| | 环数 | 3 |
| | 线条颜色 | `#C0C0C0` |
| | 高亮颜色 | `#007AFF` |
| 八卦 | 半径 | 90 |
| | 线条颜色 | `#000000` |
| | 选区透明度 | 70 |
| | 高亮透明度 | 0 |

尺寸单位为「100% 缩放下的逻辑像素」，渲染时按本屏 DPI 自动换算，
因此在任何缩放比例下视觉大小恒定。

颜色项点击色块即弹出**颜色选择器**，支持 RGB / HSV / HSL / HEX 四种格式互转。

**（2）动作**
根据「菜单样式」的选择渲染对应菜单样式，提供八个选区的动作配置。
动作选项共 16 种：
回到桌面、最小化窗口、最大化窗口、最大化/还原切换、窗口置顶切换、关闭窗口、
左半屏、右半屏、上半屏、下半屏、左上分屏、左下分屏、右上分屏、右下分屏、
左三分之二屏、右三分之二屏。

八个选区默认配置项从 12 点钟方向顺时针依次为：
最大化窗口、右上分屏、右三分之二屏、回到桌面、最小化窗口、左下分屏、左三分之二屏、左上分屏。

**（3）悬空寺**
覆盖层功能，设置项包括：是否启用、触发键（三组：左右 Alt / 左右 Shift / 左右 Ctrl）、
展示内容类型（图片 / 文字）。触发方式为**双击**触发键，按 ESC 收起。

> 数据模型里另有 Win 键与单侧左右键的枚举值，但设置页当前只暴露上述三组。

**（4）杂项**
包括：是否开机自启动（默认是）、是否最小化到系统托盘（默认是）、
触发时长（默认 200ms）。

### 【版本改动记录】
#### V0.2（当前版本）
1. 目标窗口锁定：中键按下的瞬间锁定目标窗口句柄（WindowFromPoint + GetAncestor），
   菜单弹出期间不再受前台窗口变化影响，消除误操作风险。
2. 性能优化：移除分屏时的 Thread.Sleep(50) 阻塞等待，改用 SetWindowPos + SWP_ASYNCWINDOWPOS。
3. 多显示器与高 DPI：新增 app.manifest 声明 Per-Monitor V2 DPI 感知；
   工作区改由 MonitorFromWindow + GetMonitorInfo 取物理像素，与窗口操作 API 坐标系一致；
   菜单定位增加物理像素到 WPF 逻辑坐标的换算。
4. 鼠标跟随：由菜单窗口 16ms 轮询改为鼠标钩子事件推送，跟随更精确且无空转。
5. 菜单动画：弹出缩放淡入（120ms）/ 收起淡出（80ms）。
6. 新增 3 种窗口动作：最大化/还原切换、窗口置顶切换、关闭窗口。
   新枚举值追加在 WindowAction 末尾，保证老配置文件的动作映射不错位。
7. 恢复 4 种菜单样式可选：圆环 / 八角星 / 蜘蛛网 / 八卦（此前工厂与设置页强制回退为圆环）。
8. 代码清理：移除 MainWindow、Core/MouseManager、Core/WindowActionExecutor、
   Core/RadialMenuFactory 及 MenuOverlayWindow.CalculateMenuPosition 等不可达代码。

#### V0.2 后续迭代（界面与体验）
9. **内嵌字体**：打包 Noto Sans SC（Regular + Medium），界面渲染不再依赖目标机器
   已装字体；强调字重统一用 Medium（SemiBold 会回落到 Regular）。
10. **颜色选择器**：新增独立取色窗口，支持 RGB / HSV / HSL / HEX 四种格式互转，
    来回切换格式不产生色值漂移。
11. **设置窗口改版**：固定尺寸 + PixPin 行流版式（整页白卡、等高行、1px 淡分割线），
    导航选中态为整块实心蓝 + 白字。
12. **色值框隐藏化**：界面上不再显示独立的色值文本框，色值改由色块按钮直接显示
    （控件保留作数据载体，只从视觉上摘掉）。
13. **输入框统一等宽**：宽度令牌收进统一样式，新增输入框自动同宽；
    行内单位提示文字全部移除（信息保留在 ToolTip 中）。
14. **悬空寺图片项**：标题由「键位图」改为「图片」（实际接受任意图片）；
    新增图片预览区，宽度跟随窗口自适应、等比缩放完整显示；
    未选图时展示随包分发的默认键位图。
15. **运行时自检**：新增 `--fontcheck` 与 `--settingscheck` 两个开关，
    覆盖「编译通过但运行时才暴露」的问题（如 XAML 样式类型不匹配、字体 pack URI 失效）。



