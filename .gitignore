# WinLoop - Git ignores

# --- OS / Editor ---
.DS_Store
Thumbs.db
Desktop.ini

# Visual Studio / VS Code
.vs/
.vscode/
*.user
*.suo
*.userosscache
*.sln.docstates

# Editor backup / temp files
*.tmp
*.backup
*.bak
*~
*.swp

# --- .NET build outputs ---
**/bin/
**/obj/
*.pdb
*.mdb

# Test results
TestResults/

# NuGet
**/packages/
*.nupkg

# --- WinLoop project outputs / artifacts ---
# Local build snapshots (generated)
build/

# Installer outputs (generated)
release/*.exe
release/*.zip

# Temporary working directory
tmp/

# Archived/migrated content folder (optional to keep out of repo)
deprecated/

# Reference projects cloned locally for study (not part of this repo)
._wg/
WGestures/

# Ad-hoc publish output used for local verification
probe-out/

# Logs / runtime data
*.log

# App runtime config (in case it gets created locally)
# (Actual config lives under %LOCALAPPDATA% and should not be in repo)
config.json
