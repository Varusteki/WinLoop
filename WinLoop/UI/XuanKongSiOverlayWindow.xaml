<Window x:Class="WinLoop.UI.XuanKongSiOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Left="0" Top="0"
        Width="{x:Static SystemParameters.PrimaryScreenWidth}"
        Height="{x:Static SystemParameters.PrimaryScreenHeight}">
    <Grid Background="Transparent">
        <Border Name="CardHost" CornerRadius="14" Background="#F9FBFF" Padding="20" Width="900" MaxWidth="1100"
                HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="#D0D8E8" BorderThickness="1" Opacity="0.97">
            <Border.RenderTransform>
                <TranslateTransform x:Name="CardTransform" X="0" Y="0" />
            </Border.RenderTransform>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <!-- Image -->
                    <Border Name="ImageContainer" Background="White" BorderBrush="#D0D8E8" BorderThickness="1" CornerRadius="10" Padding="10" Visibility="Collapsed">
                        <Image Name="LayoutImage" Stretch="Uniform" SnapsToDevicePixels="True" />
                    </Border>

                    <!-- Rich text -->
                    <Border Name="TextContainer" Background="White" BorderBrush="#D0D8E8" BorderThickness="1" CornerRadius="10" Padding="10" Visibility="Collapsed">
                        <RichTextBox Name="RichTextViewer" IsReadOnly="True" BorderThickness="0" Background="Transparent"
                                    VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" />
                    </Border>
                </StackPanel>

                <!-- 占位：保持布局稳定 -->
                <Border Grid.Row="1" Height="0" />
            </Grid>
        </Border>

        <!-- 操作提示：界面左下角 -->
        <Border HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="16" Padding="10,6"
                Background="#F9FBFF" BorderBrush="#D0D8E8" BorderThickness="1" CornerRadius="10" Opacity="0.95">
            <TextBlock Name="OperationHint" FontSize="13" Foreground="#4A5A73" />
        </Border>
    </Grid>
</Window>
